# Whisper Lattice (WL)

A ROOT0 / TriPod LLC artifact — a **deterministic, git-logged 5 IN -> 1 PRIM -> 1 OUT memory loop**,
dressed as a luxury object. *No AI. No GPU. Just Ada's Mill, est. 1843.*

- **`index.html`** — the front-door sphere page (the read + the working core + full .dlw badge).
- **`artifact.html`** — the latest version (v16), **scrubbed** of near-verbatim film quotes for an
  IP-clean publication (5 replacements). The 16 raw development versions are kept local.
- **`bridge/whisper_lattice_git_bridge.py`** — the working core: a local control plane that catches
  each browser event and commits it to git.
- **Live session log:** [whisper-lattice-log](https://github.com/DavidWise01/whisper-lattice-log) — the 50-cycle, 5-observer perpetuating-memory record.

Catalogued into UD0 (LOGISMÓS). CC-BY-ND-4.0 · TRIPOD-IP-v1.1.
